#include <cmath>
#include <iostream>
#include <vector>

using namespace std;

int main() {
  int T;
  cin >> T;

  vector<int> complete;

  for (int i = 0; i <= 100000 * 2; ++i) {
    pow(i, 0.5);
  }

  for (int i = 0; i < T; ++i) {
    int n, k;
    cin >> n >> k;
  }

  return 0;
}